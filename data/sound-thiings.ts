/**
 * The 3D set, one PNG per sound under `public/thiings/`.
 * thiings.co licenses individual downloads for personal use only, so this set
 * is here to be looked at, not to ship. A sound with no entry falls back to
 * the line set.
 */
export const thiingsIcons: Record<string, string> = {};
